server:
  port: 8082

spring:
  application:
    name: ocpp-charging-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    root: INFO
    com.evra.ocppcharging: INFO
